# unique

Filter (exclude) repeated lines from file or standard input

## Building

You can build the program as follows:

```bash
git clone git@github.com:claudiofsr/unique.git
cd unique
cargo build --release
```

The program will be located at `./target/release/unique`.

